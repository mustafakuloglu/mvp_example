<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greendao.mustafa.mvpexample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.greendao.mustafa.mvpexample.NoteApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.greendao.mustafa.mvpexample.login.LoginActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.greendao.mustafa.mvpexample.main.MainActivity" />
        <activity android:name="com.greendao.mustafa.mvpexample.note.NoteActivity" />
    </application>
    <meta-data
        android:name="DATABASE"
        android:value="notes.db" />

</manifest>